#!/bin/sh
# An ST4 container back into the bytes it packed: standard input in, standard
# output out, the report on standard error. README.md, The tools.
exec "$(dirname -- "$0")/st4-run" org.st4.Dst4 "$@"
